---
# Nginx Helm Chart Values - Development Environment
# Environment-specific overrides for base configuration

# Replica count for development
replicaCount: 1

# Ingress hostname
ingress:
  hostname: nginx-dev.hl.rushbtech.org

# Static content configmap
staticSiteConfigmap: nginx-dev-content

# Resources for development
resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

# Autoscaling disabled for dev
autoscaling:
  enabled: false
